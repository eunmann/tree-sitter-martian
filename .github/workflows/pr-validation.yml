name: PR Validation

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: ESLint (grammar.js)
        run: npm run lint
      - name: Prettier (formatting)
        run: npm run format:check

  test:
    name: Generate & test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci

      - name: Generate parser (committed src/ is up to date)
        run: |
          npx tree-sitter generate
          # --porcelain also catches newly-generated untracked files under src/.
          if [ -n "$(git status --porcelain -- src)" ]; then
            echo "::error::src/ is stale — run 'npx tree-sitter generate' and commit the result."
            git status --porcelain -- src
            exit 1
          fi

      - name: Corpus & highlight tests
        run: npx tree-sitter test

      - name: Parse examples (must be error-free)
        run: npx tree-sitter parse -q examples/*.mro

  upstream-corpus:
    name: Upstream Martian corpus
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci

      - name: Check out martian-lang/martian
        uses: actions/checkout@v4
        with:
          repository: martian-lang/martian
          # Pinned for reproducible CI. Bump deliberately when validating
          # against newer upstream syntax.
          ref: v4.0.15
          path: .upstream-martian
          fetch-depth: 1

      - name: Parse every upstream .mro file (no parse errors)
        run: |
          mapfile -t files < <(find .upstream-martian -name '*.mro' | sort)
          echo "Found ${#files[@]} upstream .mro files"
          if [ "${#files[@]}" -eq 0 ]; then
            echo "::error::no upstream .mro files found — checkout layout changed?"
            exit 1
          fi
          npx tree-sitter parse -q "${files[@]}"
